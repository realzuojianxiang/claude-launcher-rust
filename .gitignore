# Claude Launcher —— 仓库根 .gitignore
# 各语言实现的子目录（如 rust/）自带自己的 .gitignore，负责忽略其本地构建/依赖产物。
# 这里只负责仓库根级别、跨实现共享的忽略项。

# IDE / 编辑器
/.vscode/
/.idea/
*.swp
*.swo

# Claude Code / 代理工具的本地工作目录（会话态、缓存），不纳入版本库
/.claude/
/.agents/
/.buildtmp/
/rust/.workbuddy/

# 便携部署目录：含每次构建复制的 exe 与本地配置/凭据，不进版本库
/deploy/

# 锁文件：本地 skills 依赖锁定，随各自环境生成
/skills-lock.json

# OS
.DS_Store
Thumbs.db

# 仓库根运行时数据 / 一次性脚手架(各 session debug 产物、本地真实配置/日志、
# 个人用脚本),本地一次性或含个人数据 —— 不入库。多名而非挡 *.json/*.ps1 全局
# (防误吞真正该入库的根级配置/脚本资产)。
/config.json
/logs/
/push-rust-to-github.ps1
/docs/nvidia-proxy-mid-stream-errors-review-2026-08-11.md

# superpowers/sdd 流程产物在每个子目录产生的草稿(根 .superpowers 已挡,
# 这里补 rust/.superpowers——本次 conversation 内 rust 子目录出现的 sdd 流程
# 探查/评估草稿,本地一次性不入库)。
/rust/.superpowers/
